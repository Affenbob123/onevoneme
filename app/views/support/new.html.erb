<div class="container">
  <h3 class="text-center">Support</h3>
  <%= form_for "/contacts" do |f| %>
    <%= f.label :comments %>
    <%= f.text_area :comments %>
    <%= f.submit 'Submit' %>
  <% end %>
</div>